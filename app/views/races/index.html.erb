<p class="claim">
  runconf helps to organize conference runs<br/>
  <span class="small">(as in running from A to B for recreation)</span>
</p>

<% if @upcoming_races.any? %>
  <h1>Upcoming Races</h1>

  <ul>
    <% @upcoming_races.each do |race| %>
      <li><%= link_to race.name, race, class: 'race' %></li>
    <% end %>
  </ul>
<% end %>